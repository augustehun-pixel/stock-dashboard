## Trading Rules

매매 규칙을 구현하거나 수정하기 전에
반드시 해당 규칙 문서를 먼저 읽고 그 내용을 기준으로 작업한다.

### 매매 규칙 문서 사용 원칙

1. 매매 규칙과 관련된 기능을 구현하거나 수정하기 전에
   trading-rules/에서 해당 규칙 문서를 먼저 읽는다.

2. 작업과 직접 관련된 규칙 문서만 읽는다.
   모든 trading-rules 문서를 매번 읽지 않는다.

예:
- 돌파매매 작업 → trading-rules/breakout.md

3. trading-rules의 개별 규칙 문서를 해당 매매 규칙의
   source of truth로 사용한다.

4. 앱 코드의 기존 구현과 trading-rules 문서가 충돌하면
   임의로 판단하거나 규칙을 변경하지 않는다.
   먼저 사용자에게 충돌 내용을 알려주고 확인받는다.

5. 규칙 문서에 정의되지 않은 조건을 Claude가 임의로 추가하지 않는다.

6. 규칙의 의미가 불명확하거나 구현 방법이 여러 가지라면
   추측해서 구현하지 말고 사용자에게 질문한다.

7. 하나의 작업에서 여러 매매 규칙이 실제로 함께 사용되는 경우에만
   관련된 여러 규칙 문서를 함께 읽는다.

8. 아직 존재하지 않는 규칙 파일을 만들거나 내용을 추측하지 않는다.

### Order Block

Order Block 관련 구현, 수정, 테스트를 하기 전에
반드시 아래 문서를 먼저 읽는다.

- `docs/trading-rules/order-block.md`

Order Block에 관한 판단은 위 문서를 source of truth로 사용한다.

규칙 문서에 명시되지 않은 조건을 임의로 추가하거나
기존 규칙을 임의로 변경하지 않는다.

규칙이 모호하거나 서로 충돌하는 경우
임의로 판단하지 말고 사용자에게 먼저 확인한다.
