# 골든크로스 기준 "확정된 가장 최근 고점" 규칙 조사 — 진행 상태

이 문서는 다음 세션에서 이어서 작업할 수 있도록 현재까지의 조사 상태를 기록한 것이다.
**아직 규칙을 최종 확정하지 않았고, 이 분석 결과를 실제 매매 규칙 코드로 구현하지도 않았다.**
지금까지의 모든 검증은 `server/*.js`의 기존 함수를 재사용하는 임시(scratchpad) 스크립트로만 진행했고,
`server/` 코드 자체에는 아직 "확정된 가장 최근 고점" 판정 로직이 들어가 있지 않다.

## 우리가 만들고 있는 규칙의 목적

향후 피보나치 되돌림 등에 쓸 매매 규칙의 기준점 두 개를 정하는 것이 목적이다:

```
기준 저점 → 확정된 가장 최근 고점 → 골든크로스
```

- 피보나치 시작점(1) = 기준 저점
- 피보나치 종료점(0) = 확정된 가장 최근 고점

"확정된 가장 최근 고점"의 정확한 수학적 정의(N봉, fractal, ZigZag, % 하락 등)는 **아직 결정하지 않았다**.
지금은 실제 여러 종목의 골든크로스 사례를 모아서, 공통으로 나타나는 가격 구조를 관찰하는 단계다.

## 확정된 규칙 (변하지 않는 전제)

1. **골든크로스는 반드시 일봉 기준으로 판정한다.** 4시간마다가 아니라 거래일당 1번만 판정.
   - 전 거래일: 4시간봉 MA200 ≤ 일봉 MA200
   - 당일: 4시간봉 MA200 > 일봉 MA200
   - 4시간봉 MA200은 그 거래일의 오후(PM, 13:00~15:30) 세션 확정값만 사용 (오전 세션 사용 금지)
2. **골든크로스 이전 데이터만 사용한다.** 골든크로스 당일과 그 이후 데이터는 고점/저점 확정 과정에 **절대** 사용하지 않는다 (look-ahead 금지).
3. **기준 저점**: 골든크로스 이전 전체 구간에서 일봉 lowPrice가 가장 낮은 날.
4. **확정된 가장 최근 고점**은 반드시:
   - 기준 저점보다 나중, 골든크로스보다 이전에 위치해야 한다.
   - 골든크로스가 "발생한 시점에 이미" 고점으로 확인 가능했어야 한다 (다음날 데이터로 미래를 미리 보고 고점을 정하면 안 됨).
5. 시간 순서는 반드시 `기준 저점 → 확정된 가장 최근 고점 → 골든크로스`. `기준 저점 → 골든크로스 → 고점` 순서는 금지.

## 지금까지 탐색용으로 써본 방법 (아직 최종 채택 아님)

- "봉우리" = 전날/다음날 고가보다 오늘 고가가 더 높은 날 (가장 단순한 로컬 피크, N봉/fractal 같은 정식 정의 아님).
- 이 방법의 구조적 한계: 골든크로스 바로 전날은 "다음날" 데이터가 골든크로스 당일이라 원칙적으로 판정 불가 → 그 경우는 후보에서 자동 제외됨 (HPSP 2025-10-14 사례에서 확인).
- 후보들 중 골든크로스에 가장 가까운(마지막) 봉우리를 "확정된 최근 고점 후보"로 놓고, 그 이후 며칠간 하락이 확인됐는지 관찰.

## 지금까지 완료된 종목 (10개)

### 1. 삼성전자 (005930)
- 골든크로스: 2025-07-08
- 기준 저점: 2024-11-14, 49,900원 (2026-08-27, 2년치 데이터로 재확인 완료 — 값 동일, 이전 노트의 우려는 해소됨)
- 확정 고점 후보: 2025-07-04, 65,000원
- 고점~골든크로스 간격: 1거래일
- 하락 확인: O (1거래일, 2025-07-07: 63,500)

### 2. HPSP (403870)
- 골든크로스: 2025-10-15
- 기준 저점: 2023-01-04, 12,800원
- 확정 고점 후보: 2025-10-10, 37,500원
- 고점~골든크로스 간격: 2거래일
- 하락 확인: O (1거래일, 2025-10-13: 36,850 / 단, 2025-10-14에 38,350으로 재차 신고점 — 골든크로스 직전까지 미확정 상태로 남음. 이 케이스가 "확정 vs 미확정" 판단 기준을 정할 때 핵심 참고 사례)

### 3. SK하이닉스 (000660)
- 골든크로스: 2025-04-22
- 기준 저점: 2023-01-03, 73,100원
- 확정 고점 후보: 2025-04-14, 184,400원
- 고점~골든크로스 간격: 5거래일
- 하락 확인: O (5거래일 연속)

### 4. NAVER (035420)
- 골든크로스: 2025-01-03
- 기준 저점: 2024-08-05, 151,100원
- 확정 고점 후보: 2024-12-16, 218,000원
- 고점~골든크로스 간격: 10거래일
- 하락 확인: O (10거래일 연속)

### 5. 카카오 (035720)
- 골든크로스: 2025-03-18
- 기준 저점: 2024-11-14, 32,550원
- 확정 고점 후보: 2025-03-13, 44,750원
- 고점~골든크로스 간격: 2거래일
- 하락 확인: O (2거래일 연속 종가가 고점 고가 밑, 신고점 갱신 없음)

### 6. LG화학 (051910)
- 골든크로스: 2025-09-17
- 기준 저점: 2025-05-26, 181,500원
- 확정 고점 후보: 2025-09-15, 301,500원
- 고점~골든크로스 간격: 1거래일
- 하락 확인: O (1거래일, 신고점 갱신 없음)

### 7. 두산로보틱스 (454910)
- 골든크로스: 2025-09-30
- 기준 저점: 2023-10-27, 32,150원 (상장일이 2023-10-05라 이보다 더 과거 데이터 자체가 없음)
- 확정 고점 후보: 2025-09-25, 71,300원
- 고점~골든크로스 간격: 3거래일
- look-ahead 검증: 통과 (기준저점 < 확정고점 < 골든크로스 순서 확인)

### 8. 에코프로 (086520)
- 골든크로스: 2025-11-05
- 기준 저점: 2022-12-29, 20,150원
- 확정 고점 후보: 2025-10-30, 98,800원
- 고점~골든크로스 간격: 3거래일
- look-ahead 검증: 통과

### 9. 에코프로비엠 (247540)
- 골든크로스: 2025-10-21
- 기준 저점: 2025-05-27, 81,100원
- 확정 고점 후보: 2025-10-17, 167,400원
- 고점~골든크로스 간격: 1거래일
- look-ahead 검증: 통과

### 10. STX엔진 (077970)
- 골든크로스: **2024-05-28** (아래 "2026-08-28 세션: 4시간봉 데이터 부족 버그 발견/수정" 절 참고 — 원래는 이 골든크로스를 놓쳤다가 버그를 고친 뒤에 찾아냄)
- 기준 저점: 2023-10-24, 11,000원
- 확정 고점 후보: 2024-05-21, 14,110원
- 전날(2024-05-27): 4시간봉MA200 12,815.75 / 일봉MA200 12,818.20 / diff -2.45 (below)
- 당일(2024-05-28): 4시간봉MA200 12,831.95 / 일봉MA200 12,814.15 / diff +17.80 (above)
- look-ahead 검증: 통과

**관찰**: 골든크로스가 있는 9개 사례 모두 하락(또는 diff의 명확한 부호 전환)이 확인됐지만, 고점~골든크로스 간격이 1~10거래일로 꽤 다르다 (표본이 늘면서 "1일이면 충분"이라는 초기 가설이 흔들리는 중 — 아직 결론 내리지 않음). 이 간격을 규칙으로 확정하지 않기로 함.

## 아직 분석하지 않은 종목 (사용자가 종목 목록을 교체함, 2026-08-28)

2026-08-28 세션에서 원래 계획했던 삼성SDI/삼성바이오로직스/현대차/기아/KB금융/신한지주 6개는 검증 대상에서 제외하고, 아래 6개로 교체하기로 결정함 — 이 중 4개(두산로보틱스/에코프로/에코프로비엠/STX엔진)는 위에서 완료, 2개가 남음:

| 코드 | 이름 | 상태 |
|---|---|---|
| 454910 | 두산로보틱스 | 완료 |
| 086520 | 에코프로 | 완료 |
| 247540 | 에코프로비엠 | 완료 |
| 077970 | STX엔진 | 완료 |
| (미확인) | 한화솔루션 | **남음** — 다음 세션에서 Toss API 검색으로 정확한 코드부터 확인할 것(추측 금지) |
| (미확인) | 심텍 | **남음** — 다음 세션에서 Toss API 검색으로 정확한 코드부터 확인할 것(추측 금지) |

종목코드는 반드시 `server/index.js`의 `getStockList`/`searchStockList`와 동일한 방식(`/api/v1/stocks/all?market=KOSPI|KOSDAQ&securityType=STOCK` 전체 목록에서 이름이 정확히 일치하는 항목만 채택)으로 확인해야 한다 — 예: "에코프로"로 검색하면 "에코프로비엠/에코프로머티/에코프로에이치엔"도 함께 걸리므로 정확히 일치하는 이름만 골라야 함.

## 2026-08-28 세션: 4시간봉 데이터 부족 버그 발견/수정 (중요 — 데이터 정확성 문제)

**증상**: STX엔진(077970)을 분석했더니 2년 구간 내 골든/데드크로스가 0건으로 나왔는데, 사용자가 실제 차트에서 골든크로스를 육안으로 확인함 → 분석 결과가 실제와 다름.

**원인 조사** (look-ahead나 crossover 조건식, KST 변환, 세션(AM/PM) 선택은 전부 정상으로 확인됨. 실제 원인은 데이터 범위):
- 일봉 캐시는 2022-12-20부터, 4시간봉을 만드는 1분봉 캐시는 2024-03-26부터만 있었음 (즉 4시간봉 비교 가능 구간이 일봉보다 훨씬 늦게 시작).
- `server/fourHourCandleAnalysis.js`가 4시간봉 비교 구간을 604거래일치로만 계산하고 있었음(일봉은 900거래일). 604거래일 워밍업(100거래일)을 빼면 실제 비교 가능 구간은 2024-08-20부터 시작 — 그런데 STX엔진의 진짜 골든크로스(2024-05-28)는 그보다 3개월 전이라 애초에 "볼 수 있는 창문" 밖에 있었음.
- 1분봉을 2023년까지 확장해서(총 631,800개, TARGET_TRADING_DAYS=900 기준) 다시 계산하니 실제로 diff가 2024-05-27(-2.45, below) → 2024-05-28(+17.80, above)로 부호가 바뀌는 지점을 발견함. 골든크로스 공식/판정 로직 자체는 처음부터 정상이었음 — 단지 그 지점을 볼 수 있는 데이터가 부족했을 뿐.

**계산 근거** (604 → 900거래일로 늘린 이유):
- 일봉 MA200 워밍업: 200거래일. 일봉 확보량 900거래일 → 일봉 MA200 유효 구간 = 900 − 200 + 1 = 701거래일.
- 4시간봉 MA200 워밍업: 4시간봉 200개 = 100거래일(하루 AM/PM 2구간).
- 701거래일 전체를 4시간봉으로도 비교하려면 최소 100 + 701 = **801거래일**치 원본 데이터가 필요.
- 일봉과 동일한 900거래일로 맞추면(900 ≥ 801) 항상 충분하고, 나중에 일봉 쪽 깊이가 바뀌어도 자동으로 같이 늘어나서 다시 어긋나지 않음.

**수정 내용** (골든크로스 공식·look-ahead 규칙·최근 고점 규칙은 전혀 변경하지 않음, 데이터 확보량만 수정):
- `server/ma200Analysis.js`: `MIN_DAILY_CANDLES`(900)를 `export`로 공개해서 다른 모듈이 재사용 가능하게 함.
- `server/fourHourCandleAnalysis.js`: 자체 계산하던 `TRADING_DAYS_NEEDED`(604)를 없애고 `MIN_DAILY_CANDLES`(900)를 그대로 가져다 씀. 안 쓰게 된 `TARGET_FOUR_HOUR_CANDLES` 상수는 제거(lint 경고 방지).
- `server/minuteCandles.js`/`server/candleCache.js`는 수정하지 않음 — 기존 "캐시에 있으면 재사용, 부족한 과거분만 이어서 확장" 로직이 이미 있어서 그대로 활용됨.

**검증 결과**:
- STX엔진(077970) 재분석: 캐시 재사용으로 API 3회, 4.5초 만에 2024-05-28 골든크로스를 정확히 찾음. 전날/당일 diff 값이 사용자가 확인해준 수치와 완전히 일치.
- 회귀검증 — 두산로보틱스(454910): 골든크로스 2025-09-30, 기준저점 2023-10-27/32,150원, 확정고점 2025-09-25/71,300원 — **변동 없음**.
- 회귀검증 — 에코프로(086520): 골든크로스 2025-11-05, 기준저점 2022-12-29/20,150원, 확정고점 2025-10-30/98,800원 — **변동 없음**. (1분봉을 900거래일 깊이까지 1회성으로 확장하느라 API 1,043회, 6분 46초 소요 — 이후 재분석은 다시 캐시로 수초 내 완료)
- look-ahead 검증: STX엔진/두산로보틱스/에코프로 모두 기준저점 < 확정고점 < 골든크로스 순서 통과.
- `npm run lint`, `npm run build` 모두 통과(무관한 기존 경고 2건 제외 새 경고 없음).

**영향**: 아직 확장하지 않은 나머지 완료 종목(HPSP, SK하이닉스, NAVER, 카카오, LG화학)도 다음에 재분석할 때 1분봉을 900거래일 깊이까지 1회성으로 확장하는 비용(종목당 최대 수분)이 발생함 — 버그성 비용이 아니라 이번 수정으로 더 정확해지기 위한 정상적인 1회성 백필. 확장이 끝나면 다시 캐시로 수초 내 재분석 가능.

## 2026-08-27 세션: 속도 개선 (규칙/정의는 변경 없음)

**문제**: 종목당 분석에 10분 이상 소요.

**원인**: 1분봉(`minuteCandles.js`)은 이미 디스크 캐시 재사용 로직이 있었지만, **일봉(`dailyCandles.js`)에는 디스크 캐시가 전혀 없어서** 같은 종목을 재분석해도 매번 일봉 900개를 API에서 처음부터 다시 받아오고 있었음.

**조치**: `dailyCandles.js`에 `minuteCandles.js`와 동일한 패턴(캐시 있으면 최신 구간만 보충, 부족하면 과거만 이어서 확장)으로 디스크 캐시(`server/.cache/daily-{code}.json`)를 추가. 계산 로직은 전혀 건드리지 않음.

**효과** (실측, 2026-08-27):
- HPSP 재분석: 3.6초 → (일봉 캐시까지 예열 후) 2.4초. 결과값 완전 동일(골든크로스 2025-10-15, 기준 저점 2023-01-04/12,800원).
- 삼성전자 재분석: 3.7초. 기준 저점 49,900원 재확인(2년치 데이터로도 동일).
- 캐시가 전혀 없는 신규 종목(카카오, LG화학)은 여전히 각 800초대(약 13~14분) 소요 — 실제 과거 1분봉 데이터를 처음 받아오는 시간이라 단축 불가(속도를 위해 rate limit을 어기거나 병렬 호출을 늘리지 않기로 함).
- 일봉 페이지 요청 수: 재분석 시 5페이지 → 1페이지로 감소(오프라인 mock 테스트로 검증).

## 다음 세션 재개 지점

1. **다음 세션의 가장 먼저 할 일**: 남은 2개 종목 — 한화솔루션, 심텍 — 을 같은 방식으로 이어서 분석. 두 종목 모두 정확한 코드를 먼저 Toss API 검색으로 확인할 것(추측 금지, 위 "아직 분석하지 않은 종목" 절 참고). 새 종목은 캐시가 없어 여전히 종목당 약 13~14분 소요됨(정상 — 위 "속도 개선" 절 참고). 한 번 캐시가 쌓이면 재분석은 수초로 끝남.
2. 로컬 피크(봉우리) 탐색 로직은 scratchpad 스크립트에 있었음(세션 종료 시 사라짐) — 아래 "재사용 가능한 스크립트 로직" 절에 핵심 로직을 옮겨 적어둠(다음 세션에서 다시 타이핑할 필요 없이 그대로 재사용 가능).
3. 완료된 10개(삼성전자/HPSP/SK하이닉스/NAVER/카카오/LG화학/두산로보틱스/에코프로/에코프로비엠/STX엔진)에 한화솔루션·심텍까지 더해지면, 사례를 종합해서 "확정된 가장 최근 고점"의 공통 특징을 다시 정리하고, 그다음 사용자와 함께 최종 수학적 정의를 확정.
4. 정의가 확정된 후에야 `server/swingLevels.js`에 실제 코드로 구현 (지금은 저점 계산과 "골든크로스 이후 고점 후보 나열"까지만 구현돼 있고, "확정된 최근 고점" 자체는 아직 코드에 없음).
5. 아직 1분봉을 900거래일 깊이까지 확장하지 않은 완료 종목(HPSP/SK하이닉스/NAVER/카카오/LG화학)을 다시 분석할 일이 생기면, 위 "4시간봉 데이터 부족 버그 발견/수정" 절에서 설명한 1회성 확장 비용(종목당 최대 수분)이 발생하는 게 정상이니 놀라지 말 것.

## 재사용 가능한 스크립트 로직 (scratchpad, server/*.js에는 없음)

로컬 피크(봉우리) 후보 탐색과 하락 확인은 `server/swingLevels.js`가 아니라 scratchpad 스크립트에서만 계산했다(정의가 아직 미확정이므로 서버 코드에 넣지 않음). 핵심 로직:

```js
// 봉우리 = 전날/다음날 고가보다 오늘 고가가 더 높은 날.
// 탐색 구간: 기준 저점(exclusive) ~ 골든크로스(exclusive).
// look-ahead 금지: "다음날"이 골든크로스 당일 이상이면 그 날은 후보에서 제외.
function findLocalPeakCandidates(dailySeries, referenceLowDate, goldenCrossDate) {
  const candidates = []
  for (let i = 1; i < dailySeries.length - 1; i++) {
    const day = dailySeries[i], prev = dailySeries[i - 1], next = dailySeries[i + 1]
    if (day.date <= referenceLowDate) continue
    if (day.date >= goldenCrossDate) continue
    if (next.date >= goldenCrossDate) continue
    if (day.high > prev.high && day.high > next.high) candidates.push({ date: day.date, high: day.high })
  }
  return candidates
}
// "확정 고점 후보" = candidates.at(-1) (골든크로스에 가장 가까운 봉우리)
```

`getDailyMA200Series(code)`로 얻은 dailySeries와 `getCrossoverAnalysis(code)`의 `referenceLow`/`latestGolden`을 그대로 넣어 쓰면 된다.

## 관련 코드 (이미 구현되어 있고, 이번 조사에서 그대로 재사용 중)

- `server/swingLevels.js` — `findReferenceLow(dailySeries, goldenCrossDate)`: 기준 저점 계산 (구현 완료, 사용 중)
- `server/swingLevels.js` — `listPostGoldenCrossHighCandidates(...)`: 골든크로스 "이후" 고점 후보 나열 (구현되어 있지만 이번 조사 방향과는 무관 — 이번 조사는 골든크로스 "이전" 구간을 봄)
- `server/crossoverAnalysis.js` — `getCrossoverAnalysis(code)`: 종목별 골든/데드크로스 이벤트 목록, 정렬된 4시간/일봉 MA200 비교 데이터 제공
- `server/ma200Analysis.js` — `getDailyMA200Series(code)`: 일봉 close/high/low/MA200 시리즈 (MIN_DAILY_CANDLES=900, export됨 — fourHourCandleAnalysis.js가 재사용)
- `server/fourHourCandleAnalysis.js` — `getFourHourCandles(code)`: 2026-08-28 세션에서 `TRADING_DAYS_NEEDED`를 `MIN_DAILY_CANDLES`와 동일하게 맞춤(위 "4시간봉 데이터 부족 버그 발견/수정" 절 참고) — 일봉과 4시간봉이 항상 같은 거래일 수를 보도록 보장
- `server/candleCache.js` — 1분봉/일봉 공용 디스크 캐시 (`server/.cache/minute-{code}.json`, `server/.cache/daily-{code}.json`, git 추적 제외됨)
- `server/dailyCandles.js` — 2026-08-27 세션에서 디스크 캐시 재사용 로직 추가(위 "속도 개선" 절 참고)

## 주의 사항 (계속 유지)

- `.env`, TOSS_CLIENT_KEY/SECRET, access token은 어떤 기록/로그/캐시에도 절대 남기지 않는다.
- `server/.cache/`는 `.gitignore`에 등록되어 있어 커밋되지 않는다.
- look-ahead 금지: 어떤 저점/고점 계산도 골든크로스 당일 및 이후 날짜 데이터를 사용해서는 안 된다.
